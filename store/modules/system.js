
// state
const state = {
  windowWidth: uni.getSystemInfoSync().windowWidth // 屏幕宽度px
};
// mutations
const mutations = {};
// actions
const actions = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions
};
